# 00 · Overview (the hook)

**Goal of this segment:** set up the whole series in 30–45 seconds.

## The one-liner
"I built an AI agent that predicts the World Cup — and I'm going to show you every step, from
the idea to the spec to the code to the receipts."

## What the project is
One modular agent, three stories:
- **A — Beat the bookies:** how accurate is it, really? (numbers, scoreboard)
- **B — Model battle:** GPT vs Claude vs Gemini vs Grok, same agent, one leaderboard.
- **C — The persona:** same predictions, but the agent talks trash.

## The lifecycle we'll follow (say this on camera)
problem → spec → spec-driven build → evals → results.

## On-camera beats
1. Show the dashboard landing page (`frontend`), flick through the three tabs.
2. Show the run trace expanding on a card: "you'll literally watch it think."
3. Promise the payoff: a live scoreboard that updates as the tournament plays out.

## What to show next
Cut to `01-problem.md`.
