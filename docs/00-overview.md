# 00 · Overview (the hook)

**Goal of this segment:** set up the whole series in 30–45 seconds.

## The one-liner
"I built an AI agent that predicts the World Cup — it plays the entire knockout bracket
forward, from where we are today to who lifts the trophy."

## What the project is
One modular agent, two videos:
- **Video 1 — The Oracle:** the agent explains the signals it uses, then simulates the live
  bracket round by round to a single champion.
- **Video 2 — Model Battle:** the same bracket run by Claude, ChatGPT, Gemini, Grok and
  Composer — who do they crown, and where do they disagree?

## The lifecycle we'll follow (say this on camera)
problem → spec → spec-driven build → simulation → results.

## On-camera beats
1. Show the dashboard landing page (`frontend`), flick between the two tabs.
2. Hit **Play** and watch the bracket resolve all the way to the final.
3. Promise the payoff: re-run it every matchday as the real results come in.

## What to show next
Cut to `01-problem.md`. Full shooting scripts live in
[`scripts/video-1-oracle.md`](scripts/video-1-oracle.md) and
[`scripts/video-2-battle.md`](scripts/video-2-battle.md).
